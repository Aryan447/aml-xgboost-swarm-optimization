services:
  aml-api:
    build: .
    container_name: aml-production
    ports:
      - "8000:8000"
    volumes:
      # Mount models from host to container
      - ./models:/app/models
    environment:
      - MODEL_DIR=/app/models
    restart: always
